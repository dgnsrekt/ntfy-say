# nfty-say configuration
# Copy to .env and modify as needed

# ntfy server URL (default: https://ntfy.sh)
NFTY_SERVER=https://ntfy.sh

# Comma-separated list of topics to subscribe to (required)
NFTY_TOPICS=alerts,notifications

# Path to TTS command (supports ~/ expansion)
# Note: When using Docker, this is pre-configured in the container
NFTY_SAY=~/bin/say.sh
